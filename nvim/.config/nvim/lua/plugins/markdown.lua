return {
  "MeanderingProgrammer/render-markdown.nvim",
  dependencies = { "nvim-treesitter/nvim-treesitter", "nvim-tree/nvim-web-devicons" },
  ft = { "markdown" }, -- Only load this when you open a .md file!
  opts = {
    -- This keeps it ascetic: it hides the raw markdown symbols (like ###) 
    -- and replaces them with clean formatting.
    heading = {
      sign = false,
      icons = { "󰲡 ", "󰲣 ", "󰲥 ", "󰲧 ", "󰲩 ", "󰲫 " },
    },
  },
}
